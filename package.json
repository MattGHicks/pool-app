{
  "name": "pool-app",
  "private": true,
  "version": "0.1.0",
  "description": "PoolPilot — custom control & monitoring app for the Pentair SuperFlo VST pool pump",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  }
}
